# 基础反作弊全项检测重构说明

> 依据《基础反作弊全项检测规范》（`12.txt`，9 大类 / 51 项检测）对 AdvancedAntiCheat 进行的一次全量重构。
> 本文档是重构后的 **检测覆盖对照表**、**接线说明** 与 **差距分析** 的唯一交付文档。

---

## 一、重构目标与架构

重构围绕三层架构展开，统一了「检测项」与「违规上报」两条主链：

1. **检测目录（唯一事实来源）**：`DetectionCatalog`（`detection/core`）枚举 9 大 `Category` + 51 项 `Check`，任何"全项覆盖"统计都以它为准。
2. **违规上报契约**：`ViolationRecord.ViolationType` 枚举是各检测模块的统一上报口径，全部走
   `ViolationManager.recordViolation(...)`（分级处罚 + Web 告警 + 违规回放触发）。
3. **分层引擎**：`AdvancedDetectionManager` 分 L1（事件驱动检测）/ L2（行为画像）/ L3（关联检测）/ 融合中心。

新增模块采用 **Listener 自注册** 模式（构造函数内 `registerEvents(this)`），在
`AdvancedDetectionManager.initializeLayer1Engines()` 统一实例化即可生效。

---

## 二、51 项检测覆盖对照表

### 一、移动类（10 项）

| # | 检测项 | 目标作弊 | 实现模块 | 状态 |
|---|--------|----------|----------|------|
| 1 | 水平速度检测 | 加速/Speed | `SpeedDetection` + `MovementDetectionModule` | ✅ |
| 2 | 垂直速度检测 | 飞行/高跳 | `FlyDetection` | ✅ |
| 3 | 预测式物理模拟 | 微加速/无减速/Timer | `physics/PhysicsSimulator` | ⚠️ 部分（见差距分析） |
| 4 | 空中行为检测 | AirJump/空中加速 | `ImpossibleActionDetector.checkAirJump` | ✅ |
| 5 | 无摔落伤害检测 | NoFall | `ImpossibleActionDetector.checkNoFallDamage` | ✅ |
| 6 | 相位/穿墙检测 | Phase | `ImpossibleActionDetector.checkPhase` | ✅ |
| 7 | 水面/岩浆行走 | Jesus | `checkWaterWalk` + `AdvancedMovementDetector.checkLavaWalk` | ✅ |
| 8 | 蜘蛛攀爬检测 | Spider | `AdvancedMovementDetector.checkSpider` | ✅ |
| 9 | Timer 变速检测 | Timer | `network/ClockDriftDetector` | ✅ |
| 10 | 微时序分析 | 假延迟 | `ClockDriftDetector` + `ProtocolValidator.detectFakedDelay` | ✅ |

### 二、战斗类（9 项）

| # | 检测项 | 目标作弊 | 实现模块 | 状态 |
|---|--------|----------|----------|------|
| 11 | 攻击距离检测 | Reach | `ReachDetection` | ✅ |
| 12 | 攻击角度检测 | Aimbot/KillAura | `AdvancedCombatDetector`（`AIM_ANGLE`） | ✅ |
| 13 | CPS 检测 | 自动点击 | `CPSLimiter` / `CombatDetectionModule` | ✅ |
| 14 | 杀戮光环检测 | KillAura | `KillAuraDetection` | ✅ |
| 15 | 自瞄频谱分析 | Aimbot | `AdvancedCombatDetector`（`AIMBOT_SPECTRUM`） | ✅ |
| 16 | 击退熵分析 | AntiKnockback | `AdvancedCombatDetector`（`KNOCKBACK_ENTROPY`） | ✅ |
| 17 | 反击退相位锁定 | AntiKB | `AdvancedCombatDetector`（`ANTI_KNOCKBACK`） | ✅ |
| 18 | 自动图腾/盔甲 | AutoTotem/Armor | `InventoryDetectionModule`（`AUTO_ARMOR`） | ✅ |
| 19 | 自动喝药 | AutoPotion | `AdvancedCombatDetector`（`AUTO_POTION`） | ✅ |

### 三、挖掘与建筑类（5 项）

| # | 检测项 | 目标作弊 | 实现模块 | 状态 |
|---|--------|----------|----------|------|
| 20 | 快速破坏 | FastBreak | `FastBreakDetection` | ✅ |
| 21 | 破坏曲线一致性 | 自动矿工 | `MiningDetectionModule`（`BREAK_CONSISTENCY`） | ✅ |
| 22 | 挖掘与移动协调 | 自动挖掘 | `MiningDetectionModule`（`MINING_COORD`） | ✅ |
| 23 | 自动搭路/脚手架 | Scaffold | `ScaffoldDetection` | ✅ |
| 24 | 非法放置检测 | 任意放置 | `MiningDetectionModule`（`ILLEGAL_PLACE`） | ✅ |

### 四、背包与物品交互类（4 项）

| # | 检测项 | 目标作弊 | 实现模块 | 状态 |
|---|--------|----------|----------|------|
| 25 | 背包操作状态机 | AutoTotem/InvMove | `InventoryDetectionModule` | ✅ |
| 26 | 物品移动速度 | 自动整理 | `InventoryDetectionModule`（`ITEM_MOVE_SPAM`） | ✅ |
| 27 | 容器开关频率 | 自动仓库 | `InventoryDetectionModule`（`CONTAINER_SPAM`） | ✅ |
| 28 | 副手切换精度 | AutoTotem | `InventoryDetectionModule`（`OFFHAND_SWAP`） | ✅ |

### 五、网络与协议类（4 项）

| # | 检测项 | 目标作弊 | 实现模块 | 状态 |
|---|--------|----------|----------|------|
| 29 | 非法数据包结构 | 协议利用 | `ProtocolValidator`（`MALFORMED_PACKET`） | ✅ |
| 30 | 品牌通道验证 | 伪造客户端 | `ProtocolValidator` + `BrandChannelListener`（`BRAND_SPOOF`） | ✅ |
| 31 | 协议版本匹配 | 版本欺骗 | `ProtocolValidator`（`PROTOCOL_SPOOF`） | ✅ |
| 32 | 时钟漂移检测 | Timer | `ClockDriftDetector` + `ProtocolValidator`（`CLOCK_DRIFT`） | ✅ |

### 六、客户端与环境指纹类（3 项）

| # | 检测项 | 目标作弊 | 实现模块 | 状态 |
|---|--------|----------|----------|------|
| 33 | 渲染距离验证 | 矿透 | `ClientFingerprintModule`（`RENDER_DISTANCE`） | ✅ |
| 34 | GUI 响应时间指纹 | 注入客户端 | `ClientFingerprintModule`（`GUI_FINGERPRINT`） | ✅ |
| 35 | 隐写特征标记 | 小号追踪 | `ClientFingerprintModule` | ✅ |

### 七、蜜罐与陷阱类（5 项）

| # | 检测项 | 目标作弊 | 实现模块 | 状态 |
|---|--------|----------|----------|------|
| 36 | 幻象矿石 | 透视/矿透 | `HoneypotListener`（`X_RAY`） | ✅ |
| 37 | 幽灵实体 | ESP/杀戮光环 | `HoneypotListener`（`CHEST_ESP`） | ✅ |
| 38 | 不可能破坏进度 | 自动矿工 | `HoneypotListener.onBlockDamage`（`AUTO_MINER`） | ✅ 新增 |
| 39 | 虚假掉落物 | 自动拾取/ESP | `HoneypotListener.spawnFakeDrop`（`FAKE_DROP`） | ✅ 新增 |
| 40 | 假逃脱蜜罐 | 作弊者情报收集 | `HoneypotListener.handleFakeEscape`（`FAKE_ESCAPE`） | ✅ 新增 |

### 八、行为分析与自适应学习类（5 项）

| # | 检测项 | 目标作弊 | 实现模块 | 状态 |
|---|--------|----------|----------|------|
| 41 | 个人行为基线 | 账号共享/突变 | `BehaviorTracker` + `BehaviorAnalysisEngine` | ✅ |
| 42 | 全局异常检测 | 未知作弊 | `behavior/GlobalAnomalyDetector`（孤立森林） | ✅ 新增 |
| 43 | 操作生物特征 | 宏/脚本 | `behavior/KeystrokeDynamics`（`KEYSTROKE`） | ✅ 新增 |
| 44 | 反侦察行为检测 | 作弊者规避 | `behavior/AntiReconDetector`（`ANTI_RECON`） | ✅ 新增 |
| 45 | 动态贝叶斯融合 | 最终决策 | `fusion/BayesianNetwork` + `ProbabilityFusionEngine` + `RCPComputer` | ✅ |

### 九、关联与数据库类（5 项）

| # | 检测项 | 目标作弊 | 实现模块 | 状态 |
|---|--------|----------|----------|------|
| 46 | 设备指纹关联 | 小号 | `association/DeviceFingerprint` + `AltAccountDetector` | ✅ |
| 47 | 行为孪生匹配 | 小号 | `association/BehaviorSimilarityCalculator` | ✅ |
| 48 | 社交图谱分析 | 作弊团伙 | `association/SocialGraph` + `TeamCheatingDetector` | ✅ |
| 49 | 历史规则回溯 | 漏网之鱼 | `association/HistoricalBaselineComparator` + `OfflineRuleScanner` | ✅ |
| 50 | 风险分累计与衰减 | 累犯管理 | `AdvancedAntiCheat.startRiskDecayTask` | ✅ |

> 注：`DetectionCatalog` 中第 51 项为 `BAYESIAN_FUSION`（动态贝叶斯融合，属"最终决策"融合层，非独立违规类型），
> 上表以 50 项独立检测 + 1 项融合决策覆盖全部 51 项规范。

---

## 三、本次重构新增/修改清单

### 新增模块
| 模块 | 包路径 | 覆盖检测项 |
|------|--------|-----------|
| `GlobalAnomalyDetector` | `detection/behavior` | 全局异常（孤立森林） |
| `KeystrokeDynamics` | `detection/behavior` | 操作生物特征 |
| `AntiReconDetector` | `detection/behavior` | 反侦察行为 |
| `BrandChannelListener` | `detection/network` | 品牌通道上报接线 |

### 修改模块
| 模块 | 变更 |
|------|------|
| `AdvancedDetectionManager` | 实例化并注册全部补充检测模块 + 新增 getter |
| `HoneypotListener` | 新增 不可能破坏进度 / 虚假掉落物 / 假逃脱蜜罐 |
| `ProtocolValidator` | `getServerProtocolVersion()` 改为 public（供 BrandChannelListener 使用） |
| `ViolationRecord` | 追加 `FAKE_DROP`、`FAKE_ESCAPE` 两个违规类型（保持 ordinal 稳定） |
| `config.yml` | 补全 49 项检测开关 + `honeypot` / `fingerprint` / `behavior` 配置段 |

---

## 四、接线说明

所有新检测模块在 `AdvancedDetectionManager.initializeLayer1Engines()` 中统一实例化，
各自在构造函数内自注册事件监听；`ProtocolValidator` 通过 `BrandChannelListener` 注册
`MC|Brand` / `minecraft:brand` 通道，在客户端握手上报品牌时登记品牌与协议版本。

```text
AdvancedDetectionManager.initializeLayer1Engines()
├─ MovementDetectionModule          （已有）
├─ CombatDetectionModule            （已有）
├─ HoneypotListener                 （已有 + 3 项蜜罐扩展）
├─ AdvancedCombatDetector           （战斗补充，自注册）
├─ InventoryDetectionModule         （背包交互，自注册）
├─ MiningDetectionModule            （挖掘建筑，自注册）
├─ ClockDriftDetector               （Timer/时钟漂移，自注册）
├─ ProtocolValidator                （网络协议，含周期校验任务）
│   └─ BrandChannelListener         （品牌通道 → ProtocolValidator）
├─ ClientFingerprintModule          （客户端指纹，自注册）
├─ GlobalAnomalyDetector            （孤立森林，自注册 + 周期分析）
├─ KeystrokeDynamics                （生物特征，自注册）
└─ AntiReconDetector                （反侦察，周期任务）
```

---

## 五、差距分析（诚实声明）

以下为重构后仍存在的**已知差距**。其中 4 项已在收尾阶段关闭（见「已关闭」列），
剩余 3 项需引入外部依赖/独立基础设施（ProtocolLib / BungeeCord 沙箱子服）方可 100% 达标。

### 已关闭（本次收尾）

| 检测项 | 原差距 | 关闭方式 |
|--------|--------|----------|
| 预测式物理模拟 | `PhysicsSimulator` 占位实现（`getPlayerFromSnapshot` 恒 null、`isInLiquid`/`isGlidingWithElytra` 恒 false），未接入移动主链路 | 修复三处占位；在 `MovementDetectionModule.checkPhysicsAnomalies()` 接入：以 `getMaxSpeed()`（计入速度/缓慢药水）为单步位移上限，液体/鞘翅/速度冲量（击退、爆炸）豁免 |
| 协议版本匹配 | 仅依赖 ViaVersion，未安装则回退服务端版本 | `BrandChannelListener.resolveProtocolVersion()` 增加 Paper 原生 `Player#getProtocolVersion()` 反射回退 |
| GUI 响应时间指纹 | `probeGui()` 未自动触发 | 新增 `startAutoProbeTask()`：低频（默认 5min）仅对已有违规记录玩家弹 GUI 探针，`fingerprint.gui.auto-probe` 门控（默认关闭） |
| 隐写特征标记 | 仅用显示名混淆（`§k`），未用 NBT | 新增 `writeMarkerNbt`/`readMarkerNbt`：1.14+ 写入 `PersistentDataContainer`（全程反射），低版本自动回退显示名 |

### 仍需外部依赖/基础设施（未关闭）

| 检测项 | 差距说明 | 建议 |
|--------|----------|------|
| 非法数据包结构 | 无 ProtocolLib 时仅能对可拦截事件做反射字段校验，无法在解码层拦截原始畸形包 | 集成 ProtocolLib 做底层包校验（软依赖） |
| 假逃脱蜜罐 | 沙箱重定向依赖 `sandbox-world` 配置（默认关闭）；"伪造踢出重连 + 隔离实例"需独立实例/代理支撑，当前为情报记录 + 可选传送 | 接入 BungeeCord 沙箱子服实现完整隔离 |
| 渲染距离验证 | 以服务端替换方块 + 监听破坏实现（非发包级假方块） | 升级为协议级假方块包（需 ProtocolLib） |

---

## 六、配置说明

`config.yml` 已补全三类配置：

1. **`detection.*`**：49 项检测开关与封禁阈值，键名与 `ViolationType` 枚举名小写一一对应。
2. **`honeypot.*`**：幻象矿石 / 幽灵实体 / 不可能破坏进度 / 虚假掉落物 / 假逃脱蜜罐 的开关与参数。
3. **`fingerprint.*`**：渲染距离验证 / GUI 指纹 / 隐写标记 的开关。
4. **`behavior.*`**：全局异常 / 操作生物特征 / 反侦察 的开关（个人基线与贝叶斯融合由融合中心承载）。

> 注意：`honeypot.hologram-ore.worlds` 与 `ghost-entity.worlds` 默认 `["world"]`，
> 部署时请改为实际世界名，否则蜜罐不会在无对应世界名时生成。

---

## 七、构建验证

- 工具链：Zulu JDK 21 + Maven 3.9.9（`.mvn-run.sh` 封装，绕过 Git Bash 下 `java` 指向 Java 8 的问题）。
- 编译命令：`sh .mvn-run.sh -o compile -Ppaper`，本次重构全部新增代码已通过编译。
- 新增产物：`behavior/*.class`、`network/BrandChannelListener.class` 等均已生成。
